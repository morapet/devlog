services:
  devlog:
    build:
      context: .
      args:
        INSTALL_DRAWIO: "1"
    image: devlog:latest
    container_name: devlog
    ports:
      - "8765:8765"
    volumes:
      - ./data:/data
    restart: unless-stopped
    environment:
      DEVLOG_DATA_DIR: /data
